---
description: Implement a feature using TDD approach
allowed-tools: Bash(python:*), Read, Write, Edit, Glob, Grep
argument-hint: <feature-description>
---

Implement a new feature using Test-Driven Development:

**Feature:** $ARGUMENTS

Follow this process:
1. **Understand**: Research existing code related to this feature
2. **Design**: Propose the API/interface for this feature
3. **Test First**: Write comprehensive tests in `semantic-search/tests/`
4. **Implement**: Write the implementation to make tests pass
5. **Verify**: Run all tests to ensure nothing is broken
6. **Document**: Update relevant docstrings and CLAUDE.md if needed

Use the TodoWrite tool to track progress through these steps.
